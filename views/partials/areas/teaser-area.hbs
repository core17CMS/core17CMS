<div class="area teaser-area">
    {{#each viewData}}
        <div>
            {{#each this}}
                <div>
                    {{> (lookup . 'elementName') viewData=this}}
                </div>
            {{/each}}
        </div>
    {{/each}}
</div>
